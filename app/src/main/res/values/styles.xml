<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="textInputStyle">@style/Widget.Design.TextInputLayout</item>
        <item name="cardViewStyle">@style/CardView</item>
       </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>


    <style name="text1">
        <item name="android:maxLines">1</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textColorHint">@android:color/white</item>
        <item name="android:label">@string/registration</item>
        <item name="android:drawablePadding">10dp</item>
        <item name="android:singleLine">true</item>
        <item name="android:inputType">number</item>
        <item name="android:drawableLeft">@drawable/mob</item>
    </style>
    <style name="cardText" parent="TextAppearance.AppCompat">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/card_background</item>
        <item name="android:textSize">@dimen/cardtext_size</item>
        <item name="android:layout_margin">@dimen/padding</item>
        <item name="android:fontFamily">times new roman</item>
    </style>
    <style name="reportText" parent="TextAppearance.AppCompat">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">@dimen/cardtext_size</item>
        <item name="android:layout_margin">@dimen/padding</item>
        <item name="android:fontFamily">times new roman</item>
    </style>
    <style name="text2">

        <item name="android:textColor">@android:color/black</item>
        <item name="android:textColorHint">@android:color/white</item>
        <item name="android:label">@string/mobile</item>
        <item name="android:singleLine">true</item>
        <item name="android:inputType">numberPassword</item>
        <item name="android:drawablePadding">10dp</item>
        <item name="android:drawableLeft">@drawable/ic_lock_open_black_24dp</item>
    </style>

    <string name="username">Enter Number</string>
    <string name="password">Enter OTP</string>
    <string name="mobile">Enter Parent Mobile Number</string>
    <string name="registration">Enter registration Id</string>
    <dimen name="cardtext_size">14sp</dimen>
</resources>
